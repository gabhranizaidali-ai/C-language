

#include <stdio.h>

int main()
{
    // c language has total 32 reserved keywords

    // keyword are the word which we can't use as variable and function name in our program

    // because keyword functionality already defined for specific task that's why

    //  if you still use the conflict occur in compiler can't able to understand what to do when same name function and variable have in our program

    // int int = 24;

    // float float = 10.2;

    return 0;
}