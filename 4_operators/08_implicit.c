

#include <stdio.h>

// compiler will be convert automatically the data type

int main()
{

    float a = 10.20;

    int b = 30;

    printf("\n sum of these two number is %d", a + b);

    printf("\n sum of these two number is %d", (int)a + b);

    return 0;
}